# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-server
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Storybook
storybook-static

# Build artifacts
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# Claude Code local settings
.claude/settings.local.json
.claude/worktrees/
.claude/scheduled_tasks.lock

# Project-specific GIS data and loaders (user-provided)
/GISData/
/docker/seed/load-shapefiles.sh

# Local config / infra (not committed)
/config.json
/TODO.md
/ansible/
/terraform/
/terraform-gcp/
/scripts/

# Local-only investigation artifacts (raw DB dumps, logs, screenshots
# captured during feedback triage — may contain credentials)
/INVESTIGATION.md
/investigation/
/git_hours.py

# AI round-trip QA scratch (exports/captures)
.qa-tmp/

# Playwright MCP browser snapshots (scratch)
.playwright-mcp/
